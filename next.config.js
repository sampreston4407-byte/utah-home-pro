/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel handles SSG natively — no output: 'export' needed.
  // To generate a fully static HTML export for other hosts, uncomment:
  // output: 'export',
}

module.exports = nextConfig
